"""
Preprocessing steps:
    - reshape the data into days (extended days for T1DMS)
    - split the data into train, valid and test sets
    - (low pass filter the data)
    - min max normalization
"""